# Vite dev proxy target (used in vite.config.js)
VITE_API_URL=http://localhost:8000

# Optional runtime API base URL for frontend requests.
# Keep empty for same-origin deployments.
VITE_API_BASE_URL=
